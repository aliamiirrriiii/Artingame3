# The app is a WebView shell with no reflection-sensitive code, so the defaults
# are sufficient. Kept so `assembleRelease` works with minify enabled if wanted.
-keepattributes SourceFile,LineNumberTable
